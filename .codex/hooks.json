{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[security-recon] Rode: claude -p \"use o subagente security-recon para varrer este repo\" antes de finalizar mudanças sensíveis de auth/pagamento/RLS.'"
          }
        ]
      }
    ]
  }
}
